id: c20
type: chore
title: pitch to README (EN+RU) — the two-paragraph why + who it is not for
status: done
priority: high
tags:
    - docs
    - release
created: "2026-07-27T15:50:58Z"
updated: "2026-07-28T01:53:36Z"
description: 'Pre-1.0 polish (Variant A). Put the two-paragraph pitch into README (EN and RU): the pain (an agent silently skips unenforced checks and lies about done) and what mtt does (per-type flow whose transitions run your shell checks as blocking gates; the status cannot lie). Include who it is NOT for (just want a to-do list -> use something lighter) and both faces of the wedge: agent-cannot-lie AND human-sees-true-state via show/list/roadmap. Align with the existing root tagline. Source text: KB note pitch.'
history:
    - at: "2026-07-27T17:14:49Z"
      by: Grigorii Pashukhin
      from: tbd
      to: implementing
      checks:
        - cmd: git switch task/c20 || (git switch main && git switch -c task/c20)
          exit: 0
        - cmd: test -f .mtt/tasks/c20.yaml || { echo "task file absent on this branch — its add has not reached main (merge/commit the branch that created it first)" >&2; false; }
          exit: 0
    - at: "2026-07-27T17:17:21Z"
      by: Grigorii Pashukhin
      from: implementing
      to: impl_review
      checks:
        - cmd: out=$(git status --porcelain -- ":(exclude).mtt") && test -z "$out" || { echo "working tree not clean - commit your code/docs first (.mtt is swept by the move itself)" >&2; false; }
          exit: 0
        - cmd: 'git diff --quiet main...HEAD -- cmd internal pkg go.mod go.sum || git diff --name-only main...HEAD -- CHANGELOG.md | grep -q . || { echo "code changed but CHANGELOG.md has no entry - add one under [Unreleased] (pure refactor? bypass: mtt do submit --no-run --who ... --why ...)" >&2; false; }'
          exit: 0
        - cmd: make check
          exit: 0
    - at: "2026-07-27T17:21:12Z"
      by: Grigorii Pashukhin
      from: impl_review
      to: approved
      checks:
        - cmd: out=$(git status --porcelain -- ":(exclude).mtt") && test -z "$out" || { echo "working tree not clean - commit your code/docs first (.mtt is swept by the move itself)" >&2; false; }
          exit: 0
    - at: "2026-07-28T01:53:36Z"
      by: Grigorii Pashukhin
      from: approved
      to: done
      checks:
        - cmd: git switch main
          exit: 0
        - cmd: 'git log -n 1000 --format=%s | grep -v "^mtt: " | head -200 | grep "^c20: " || { echo "no squash commit \"c20: …\" on local main: git pull first, and check the PR/merge title started with \"c20: \"" >&2; false; }'
          exit: 0
