id: t58
type: task
title: 'integrity upgrades: check covers dangling depends_on and duplicate ids; roadmap marks missing blockers; dep --cycles exits non-zero'
status: spec_review
tags:
    - core
created: "2026-07-22T08:01:24Z"
updated: "2026-07-25T02:02:21Z"
description: 'Audit findings (2026-07-22): mtt check inspects refs only — a dangling depends_on (the one edge type that blocks work) passes silently; roadmap prints blocked by: tX for a nonexistent id with no (missing) marker while dep list marks it; a duplicated id across task files (hand-copied yaml keeping the inner id) is a silent split-brain — list shows duplicate rows, show by filename-id returns the other task object, edits desync the copies, check stays silent; dep list --cycles finds a hand-edited cycle but exits 0, so unlike check (exit 7) it cannot gate CI. Design open: new finding classes under exit 7 vs a new code; --cycles exit semantics aligned with check.'
history:
    - at: "2026-07-25T01:53:15Z"
      by: grisha
      from: tbd
      to: speccing
      checks:
        - cmd: git switch task/t58 || (git switch main && git switch -c task/t58)
          exit: 0
        - cmd: test -f .mtt/tasks/t58.yaml || { echo "task file absent on this branch — its add has not reached main (merge/commit the branch that created it first)" >&2; false; }
          exit: 0
    - at: "2026-07-25T02:02:21Z"
      by: grisha
      from: speccing
      to: spec_review
      checks:
        - cmd: ls docs/superpowers/specs/t58-*.md
          exit: 0
        - cmd: out=$(git status --porcelain -- ":(exclude).mtt") && test -z "$out" || { echo "working tree not clean - commit your code/docs first (.mtt is swept by the move itself)" >&2; false; }
          exit: 0
