id: t62
type: task
title: 'ship runnable init templates: universal domain-neutral flow samples (content/approval/generic script-gate) + the git-integration flagship after t24'
status: plan_review
priority: medium
tags:
    - dx
    - flow
refs:
    - kind: task
      id: t32
    - kind: task
      id: t33
created: "2026-07-23T03:58:11Z"
updated: "2026-07-25T10:33:38Z"
description: |-
    Split out of t57 (2026-07-23 brainstorm). t57 ships the flow-authoring GUIDE (docs-only); this ships the runnable, discoverable SAMPLES.

    Two parts:
    1. Universal, domain-neutral mtt init --template <name> samples - simple but extensible - that demonstrate mtt generality: a flow binds transitions of your config-defined entities to external actions/artifacts (a script run, a file write, a git op, an HTTP call), and running a test suite is just ONE instance. Candidate set (decide in this task own design): a content-review flow, an approval/sign-off flow, and a generic script-gated flow (a placeholder external action). Each: template YAML + templates.go registration + golden test + testscript e2e + a CLI_REFERENCE line. NOT blocked by t24.
    2. The git-integration FLAGSHIP template (our dogfood flow: branch/auto-commit/PR/deliver) - deferred until t24 (global/default post: actions) collapses the ~7x duplicated post blocks, so the shipped template is clean.

    The FLOW_GUIDE (t57) forward-links here. Keep templates minimal and clearly labelled as adaptable samples, per the t57 framing (the engine is the product; a flow is per-project data; our flow is a sample, not a mandate).

    Release bar (decided 2026-07-24, client-responsibility discussion): NO SILENT TRAPS. Every external assumption of a shipped template (pushable main, squash_merge_commit_title=PR_TITLE, gh+jq presence) is either checked by a gate or stated in the description of the edge that relies on it; state-moving edges carry the --no-run caveat in their own descriptions (bypass skips ALL commands including context moves - the client owns their commands under bypass exactly as under execution); templates ship runnable and tested like demo/. Client-facing halves of t32 (bypass caveat) and t33 (protected-main assumption) are absorbed here as template-content duties - the engine stays a dumb mechanism.

    Design input (2026-07-24, user - weigh in the brainstorm, NOT pre-decided): maybe the sample SET is premature. Alternative shape: ship ONE minimal built-in template - new -> {in_progress, cancelled}, in_progress -> {done, cancelled} (note: the current flat default from t23 is already close to this) - and let the user supply their own template at init time: mtt init --template <path-or-url> (a local file path or a URL). Implications to weigh: external templates shift authoring to the client (fits the engine-not-mandate framing and shrinks our template-maintenance surface); a URL fetch adds a network touchpoint to an otherwise zero-network binary (t44 self-update is the only precedent - decide deliberately, offline-first); a fetched template is config-as-code from an UNTRUSTED source, so the SEC2 review-before-run discipline must be loud in the UX; the embedded default/coding/hierarchy set and the FLOW_GUIDE forward-link get rescoped accordingly.
history:
    - at: "2026-07-25T06:52:56Z"
      by: Grigorii Pashukhin
      from: tbd
      to: speccing
      checks:
        - cmd: git switch task/t62 || (git switch main && git switch -c task/t62)
          exit: 0
        - cmd: test -f .mtt/tasks/t62.yaml || { echo "task file absent on this branch — its add has not reached main (merge/commit the branch that created it first)" >&2; false; }
          exit: 0
    - at: "2026-07-25T09:12:17Z"
      by: Grigorii Pashukhin
      from: speccing
      to: spec_review
      checks:
        - cmd: ls docs/superpowers/specs/t62-*.md
          exit: 0
        - cmd: out=$(git status --porcelain -- ":(exclude).mtt") && test -z "$out" || { echo "working tree not clean - commit your code/docs first (.mtt is swept by the move itself)" >&2; false; }
          exit: 0
    - at: "2026-07-25T09:22:16Z"
      by: Grigorii Pashukhin
      why: 'adversarial spec review DECLINE: (BLOCKER) de-embedding coding breaks demo/coding-flow.sh -> make check red, demo/ not in migration scope; (MAJOR) validate-before-write named Config.Validate but prefix/timeout/exactly-one-default live in adapter checkPrefixes incl the letters-only shell-safety prefix rule -> need a shared bytes-decode running the FULL load-path checks (Load reads file, no bytes entry today); (MAJOR) migration undercounted: --template hierarchy in 11 testscripts + init_test + README/DESIGN/FLOW_GUIDE EN+RU, not just roadmap.txt; (MAJOR) TTY confirm + fetch seams under-specified vs t44 fake-httpDoer no-socket precedent; (MINORs) LimitReader silent-truncate, no non-200 check, --name silently ignored for external, git-flow stated-assumptions untestable + runnable-demo scope, classification/x-term dep.'
      from: spec_review
      to: spec_fix
    - at: "2026-07-25T09:26:02Z"
      by: Grigorii Pashukhin
      from: spec_fix
      to: spec_review
      checks:
        - cmd: ls docs/superpowers/specs/t62-*.md
          exit: 0
        - cmd: out=$(git status --porcelain -- ":(exclude).mtt") && test -z "$out" || { echo "working tree not clean - commit your code/docs first (.mtt is swept by the move itself)" >&2; false; }
          exit: 0
    - at: "2026-07-25T09:30:51Z"
      by: Grigorii Pashukhin
      why: 'spec re-review: all rev1 findings resolved, but rev2 introduced (MAJOR) "Load refactors to os.ReadFile->DecodeConfig" misdescribes Load — it is a TWO-FILE overlay (config.yaml + config.local.yaml later-wins) + pre-overlay committedRequire capture + tighten-only require + Author from overlay; a one-slice DecodeConfig cannot express that and would drop the overlay/attribution. Fix: shared checkDecoded(yc) after the overlay; external path = ValidateTemplateBytes(single doc). (MINOR) fake httpDoer.Do bypasses http.Client CheckRedirect so redirect-to-http-refuse untestable — inject http.RoundTripper into a real client + pure httpsOnlyRedirect predicate. (MINOR) gh keyword substring false-positives (through/high) -> token match; scheme-less-URL hint misfires on ./x.yaml -> exclude leading ./../ .'
      from: spec_review
      to: spec_fix
    - at: "2026-07-25T09:33:08Z"
      by: Grigorii Pashukhin
      from: spec_fix
      to: spec_review
      checks:
        - cmd: ls docs/superpowers/specs/t62-*.md
          exit: 0
        - cmd: out=$(git status --porcelain -- ":(exclude).mtt") && test -z "$out" || { echo "working tree not clean - commit your code/docs first (.mtt is swept by the move itself)" >&2; false; }
          exit: 0
    - at: "2026-07-25T09:37:58Z"
      by: Grigorii Pashukhin
      why: 'spec re-review: rev2 items all closed; one new MINOR — checkDecoded as written includes Config.Validate but Load today does NOT run it (load.go:45 "domain invariants are the caller`s"; only add.go/types.go call Validate), so routing Load through it would change read-path behavior on ~24 callers. Fix (reviewer-prescribed): checkDecoded = toDomain+checkPrefixes+parseCommandTimeout only (Load byte-identical); Config.Validate lives in the external ValidateTemplateBytes path alone (init owns the domain check for an untrusted template).'
      from: spec_review
      to: spec_fix
    - at: "2026-07-25T09:38:53Z"
      by: Grigorii Pashukhin
      from: spec_fix
      to: spec_review
      checks:
        - cmd: ls docs/superpowers/specs/t62-*.md
          exit: 0
        - cmd: out=$(git status --porcelain -- ":(exclude).mtt") && test -z "$out" || { echo "working tree not clean - commit your code/docs first (.mtt is swept by the move itself)" >&2; false; }
          exit: 0
    - at: "2026-07-25T09:40:44Z"
      by: Grigorii Pashukhin
      from: spec_review
      to: spec_human_review
    - at: "2026-07-25T09:51:49Z"
      by: Grigorii Pashukhin
      why: 'human sign-off: spec accepted (rev4) after 3-round adversarial review'
      from: spec_human_review
      to: planning
    - at: "2026-07-25T10:12:02Z"
      by: Grigorii Pashukhin
      from: planning
      to: plan_review
      checks:
        - cmd: ls docs/superpowers/plans/t62-*.md
          exit: 0
        - cmd: out=$(git status --porcelain -- ":(exclude).mtt") && test -z "$out" || { echo "working tree not clean - commit your code/docs first (.mtt is swept by the move itself)" >&2; false; }
          exit: 0
    - at: "2026-07-25T10:27:01Z"
      by: Grigorii Pashukhin
      why: 'adversarial plan review DECLINE (verified): (BLOCKER) migration surface grossly undercounted — a --template-hierarchy string grep misses ~9 Go test files that use the built-in NAME via Init(root,"hierarchy"/"coding") or runRoot(...,"init","--template","hierarchy") as separate args: initHierarchy helper (task_test:171, ~20 tests in task_test/task_security/perm_unix), init_test golden/unknown/Init tests, load_test:12, and ~8 cli tests (add/edit/list/project/show_json/show). (BLOCKER) coding/hierarchy carry unquoted name: {{.Name}} = invalid standalone YAML → verbatim file-path install fails to parse; must de-template to a literal (default stays {{.Name}} as the rendered built-in; TestExamplesLoadAndValidate must exclude default). (MAJOR) Task6 ordering assertion loops {task,chore} but chore has no spec_review/plan stages. (MAJOR) init.txt ends with -- empty/.keep -- so appended cmds are swallowed (false-green). +MINORs: missing redirect-through-real-client + --name-note tests, confirm.go imports, erroneous git rm -r after git mv, demo/doc.go.'
      from: plan_review
      to: plan_fix
    - at: "2026-07-25T10:33:38Z"
      by: Grigorii Pashukhin
      from: plan_fix
      to: plan_review
      checks:
        - cmd: ls docs/superpowers/plans/t62-*.md
          exit: 0
        - cmd: out=$(git status --porcelain -- ":(exclude).mtt") && test -z "$out" || { echo "working tree not clean - commit your code/docs first (.mtt is swept by the move itself)" >&2; false; }
          exit: 0
