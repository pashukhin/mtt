# build output
/bin/
/dist/
/mtt

# test / coverage artifacts
*.out
coverage.*

# editor / OS
.DS_Store
*.swp

# personal Claude Code settings (per-user, not shared)
.claude/settings.local.json

# personal per-project mtt config: connection params, local prefs (overrides .mtt/config.yaml)
.mtt/config.local.yaml
