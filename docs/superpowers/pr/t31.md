## What

Retires the pre-mtt session apparatus and closes the bypass channels agents were still following
(investigated + brainstorm-decided 2026-07-23; spec/plan in `docs/superpowers/{specs,plans}/t31-*`):

1. **KB seed** — 12 curated notes distilled from NEXT_SESSION carry-over lessons and the analysis
   notes; the 5 high-priority ones are the `mtt prime` session-start digest. The KB was empty despite
   t47/t51 shipping.
2. **Flow mechanization** (`.mtt/config.yaml`, guard-test-first): a fail-closed **clean-tree gate**
   on all 8 submit edges and both `impl_review → approved` edges (`.mtt` excluded — the move's own
   post-commit sweeps it); a **CHANGELOG gate** on the 4 impl submits (resolves t54); a KB-capture
   reminder on `deliver`. All three ran live on this task's own moves (blocked probes + green real
   runs recorded in t31's history).
3. **Purge** — `NEXT_SESSION.md` (stale "living handoff doc" teaching the retired lifecycle),
   `TASKS.md` (frozen), `sessions/` (30 files), and all delivered-task artifacts under
   `docs/superpowers/` (~100 files). Git history keeps everything; four session specs cited by live
   DESIGN Shipped blocks are deferred to t63.
4. **Pointer rewiring** (EN+RU) — "see TASKS.md → Later" citations now point at live mtt ids
   (t3/t10/t11/t18/t36); the DESIGN flow-fact gate inventory updated; README's TASKS.md bullet became
   the dogfood line.
5. **mtt-first rules** (AGENTS.md + CLAUDE.md) — closure only via flow edges (`rm` ≠ closure; "done
   directly" forbidden — already-decided work becomes a `chore`); knowledge → `mtt note add`; the
   only "what's next" source is `mtt roadmap`; superpowers activation inlined into CLAUDE.md.

Queue changes riding the branch: t64 (test-suite gap batch), t65 (artifact signing) filed from the
2026-07-10 audit; t63 extended to own the four deferred specs. After delivery: `cancel t53 t54 --why`.

## Verification

- `make check` green; `TestRepoDogfoodConfig` asserts the new gate scalars byte-for-byte (updated
  red-first).
- Both new gates observed blocking live (dirty tree → exit 3 on `submit` and on `approve`) and
  passing on the real moves; the CHANGELOG gate passed via this PR's own `[Unreleased]` entry.
- `mtt prime` = the 5 high notes; `mtt note list` = 12; zero markdown links to deleted files
  (`git grep -E '\]\((TASKS\.md|NEXT_SESSION\.md|sessions/)'` empty).
- Three adversarial subagent reviews (spec, plan, impl) — each round's findings fixed and re-verified;
  impl verdict APPROVE.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
