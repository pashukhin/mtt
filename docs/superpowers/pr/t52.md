## What

`mtt init` (by default) and a new **`mtt agent hooks`** command scaffold a coding agent's configuration into
the project, per the approved spec (`docs/superpowers/{specs,plans}/t52-agent-hooks-scaffold.md`; **4
adversarial spec rounds + 3 plan rounds + an impl review**, all APPROVE-CLEAN at the end). Continues the
post-t62 onboarding arc; sibling of t46 (agent **docs**, deferred).

1. **`internal/fsutil` (extracted, DRY)** — the yaml adapter's atomic-write + dir-fsync discipline moves into a
   shared `fsutil.AtomicWrite(path, data, fallbackMode)` / `fsutil.SyncDir(dir)`. The adapter now delegates
   (`atomicWrite`/`atomicWriteMode` are thin wrappers; `audit.go` calls `fsutil.SyncDir` directly) — behavior
   **byte-identical** (perm-preservation, zero-byte-reserve, platform fsync handling all move verbatim), pinned
   by the adapter's existing tests/goldens. The second consumer (the scaffold) justified the extraction.
2. **`internal/scaffold` (the seam + merge)** — a `Harness` interface + `Registry()` (only `claude`; codex/gemini
   deferred, **no guessed config written**). The pure `claudeHarness.Merge` computes `.claude/settings.json`:
   `SessionStart`+`PreCompact` hooks running `mtt prime 2>/dev/null || true` + a **read-only `permissions.allow`**
   (granular per read-only subcommand; every mutating verb excluded). The merge is **additive, idempotent,
   no-clobber** — one `json.Encoder` (`SetEscapeHTML(false)` keeps `2>/dev/null` literal; 2-space indent;
   alphabetical keys = the serializer's fixed point → a re-run is a byte-identical `Unchanged`); presence is a
   **word-boundary `mtt prime` prefix** (robust to customized args, not fooled by an incidental mention); a
   malformed/type-incompatible existing file is a **loud refusal**, an empty/`null` container reads as absent.
   `Run` is the thin IO edge (write-as-you-go; only `os.IsNotExist` ⇒ absent).
3. **`mtt agent hooks`** — a new `mtt agent` cobra group (mirrors `note`/`dep`) over `scaffold.Run`, root via
   `projectRoot` (an initialized project). Prints `claude: created|merged|unchanged <path>`; `--json` emits
   `[{harness, path, action}]` (shared `reportScaffold`/`scaffoldJSON`, reused by init).
4. **`mtt init` scaffolds by default** — `--no-agent-hooks` opts out; `init --json` gains a non-null `scaffold`
   array. **Failure contract (R3):** config (the primary job) is written first and **kept**; a scaffold failure
   reports the config success, then exits 1 (no partial JSON, no rollback).
5. **Docs (EN+RU)** — a Shipped(t52) note in DESIGN(+ru); the stale t51 "hook is config, not code" claim flipped
   everywhere it restates (DESIGN(+ru), cli/CLAUDE.md); FLOW_GUIDE(+ru) "Settings & hooks" bullet flipped to
   shipped; CLI_REFERENCE(+ru) (`mtt agent hooks` + `--no-agent-hooks`); new `internal/fsutil/CLAUDE.md` +
   `internal/scaffold/CLAUDE.md`; README(+ru); CHANGELOG.

## Verification

- **TDD red→green per task**; `make check` green before every commit and at each task boundary (the fsutil
  extraction and the scaffold land before the CLI wiring, so no task is ever left red).
- **Driven on the real binary** in a temp project: `mtt init` scaffolds + prints `claude: created` + the review
  note; `.claude/settings.json` has literal `2>/dev/null`, alphabetical keys, both events, the allowlist; a
  re-run of `mtt agent hooks` → `unchanged`; `--json` emits the result array.
- **Adversarial code review: APPROVE-CLEAN** (no blocker/major; `make check` green). Three folded minor
  test-coverage strengthenings: `init --no-agent-hooks --json` asserts `"scaffold": []`; the malformed-init
  `--json` path asserts no partial JSON object; the odd-shape/word-boundary merge tests now assert the hook is
  appended and the original command preserved.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
