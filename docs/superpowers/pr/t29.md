## What & why (t29, `[dx]`)

Where should an agent's **pre-flow knowledge** live — queue navigation, first-move setup, mid-flight
resumption? The task floated a `mtt guide` command. On inspection every scenario is **already served**:
navigation by `roadmap`/`ready`/`types`, resumption by `use` + `show` (which prints the `▸` status guidance +
`next:` moves — the t19 self-instructing flow), and first-move by the README Quickstart. So a `guide` would
just restate existing commands (DRY/YAGNI), and one-time setup belongs in docs, not the software.

## Decision (record: `docs/superpowers/specs/t29-preflow-knowledge-home.md`)

**No `mtt guide`/`resume` command.** Pre-flow knowledge has three homes:
- **README** — *learn the tool* (Install + Quickstart). One-time; any polish is **t42**.
- **the `mtt init`-scaffolded runbook** — *how the adopter's agents work under mtt* (the maturation loop,
  attribution, branch/PR). Scaffolded by **t23** (which depends on t29 and inherits this split).
- **root-help** — always-on in-tool orientation: navigation **+ resumption**.

## Change (the only code)

The root `Long` oriented only a *fresh* start ("Start with `roadmap`/`ready`/`types`"). Add a **resumption**
clause so a returning agent learns the path from the CLI itself:

> …resuming, `mtt use` shows your current task and `mtt show` its status + next moves.

A help-text test (`TestRootHelpMentionsResume`) pins it, keyed on `resuming`/`mtt show` (unique to the new
clause — `mtt use` already appears in the Shorthand sentence). No behavior change; no other command touched.

## Reviews

- **spec_review** PASS — the "already covered" claim was verified against the code (`show` really prints
  `▸`/`next:` via the t19 guidance; `use` shows the pointer; README has Install+Quickstart; **t23 depends on
  t29**). 2 nits folded (test-token uniqueness; wording).
- **plan_review** PASS — the exact one-line edit was applied and verified end-to-end (red-before/green-after,
  `make check`, no existing help test breaks).
- **impl_review** — self-review (proportionate for a one-clause help-string change plan_review already
  applied+tested): new test green, `TestRootHelpMentionsSugar`/`Short` intact, `make check` green, live
  `--help` shows the clause.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
