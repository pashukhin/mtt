## What

`mtt agent docs` (and `mtt init` by default) scaffold a **project-agnostic** "Working under mtt" runbook into
a project, per the approved spec (`docs/superpowers/{specs,plans}/t46-agent-docs-scaffold.md`; **1 spec round +
1 plan round + an impl review**, all APPROVE-CLEAN). The docs sibling of t52; continues the onboarding arc.

1. **Seam consolidation (pre-release, DRY).** t52 named the `internal/scaffold` seam `Harness` assuming every
   target is an agent-tool config; t46 adds `AGENTS.md` (a cross-tool doc, not a harness), so the honest names
   land: `Harness`→**`Target`**, `Registry()`→**`HookTargets()`**, `Result.Harness`→**`Result.Name`**; the CLI
   JSON `{harness}`→**`{name}`** and `init --json`'s `scaffold` array → **`hooks`**+**`docs`**. Behaviour-
   identical (t52's tests updated to the new key names; `make check` green).
2. **Doc scaffolding.** A pure **`blockMerge`** injects a `<!-- mtt:begin -->…<!-- mtt:end -->` block:
   create-if-absent, **append** to an existing file, **regenerate** on re-run (so it stays current as mtt
   evolves), refuse a mangled marker pair — **all surrounding content preserved, never a clobber**.
   **`DocTargets()`** are three targets: `AGENTS.md` (the full runbook) + `CLAUDE.md`/`GEMINI.md` (thin
   pointers, sharing content). Content is **`//go:embed`**-ed markdown (`docdata/`; backticks preclude a Go raw
   literal). The runbook is **generic**: it describes mtt's mechanics and directs the agent to
   `mtt types`/`mtt roadmap`/`mtt show` for *this* project's flow — it hardcodes **no** status and assumes
   **no** named edge (it points at `mtt show <id>`'s `next:`).
3. **`mtt agent docs`** — a sibling of `mtt agent hooks` in the `mtt agent` group (root via `projectRoot`).
   `--json` emits `[{name, path, action}]`.
4. **`mtt init` scaffolds docs by default** — `--no-agent-docs` opts out; `init --json` gains a non-null
   `docs` array. The config-then-scaffold **failure contract** (config written & kept, exit 1, no partial JSON)
   extends to a second phase: config → hooks → docs; a scaffold failure reports config-success + everything
   that landed, then exits 1.
5. **Docs (EN+RU)** — Shipped(t46) note in DESIGN(+ru); FLOW_GUIDE(+ru) "Agent-usage docs" bullet flipped to
   shipped (naming all three files); CLI_REFERENCE(+ru) (`mtt agent docs` + `--no-agent-docs`, and the t52
   `agent hooks`/`init` `--json` key sweep); `internal/scaffold/CLAUDE.md` + `internal/cli/CLAUDE.md`;
   README(+ru); CHANGELOG.

## Verification

- **TDD red→green per task**; `make check` green before every commit and at each task boundary (the rename +
  the doc facet land before the CLI wiring, so no task is left red).
- **Driven on the real binary** in a temp project: `mtt init` scaffolds config + hooks + docs (both notes);
  `AGENTS.md` opens with the marked block + runbook; re-running `mtt agent docs` → all `unchanged`; a merge
  into a hand-edited `AGENTS.md` → `merged`, preserving the user's content AND adding the block.
- **Adversarial code review: APPROVE-CLEAN** (`make check` green, 0 lint issues). Two folded minors: the
  package doc comment was refreshed to the new `Target`/`HookTargets`/`DocTargets` names, and a docs-phase
  failure now reports the doc targets that landed before it (pinned by a new e2e case).

🤖 Generated with [Claude Code](https://claude.com/claude-code)
